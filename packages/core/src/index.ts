/**
 * meetAI domain types. Everything here is framework-free and storage-free:
 * no HTTP, no SQL, no model calls. If a type needs an import from an app,
 * it does not belong in this package.
 */
export type { Proposition } from "./proposition.ts";
