{
  "name": "@meetai/evals-propositions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Eval harness for proposition extraction: fixtures, per-window checks, manual verdict loop",
  "scripts": {
    "eval": "bun src/run.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@meetai/ai": "workspace:*",
    "@meetai/core": "workspace:*",
    "@meetai/ingestion": "workspace:*",
    "@t3-oss/env-core": "^0.13.11",
    "qwen-proxy": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@meetai/tsconfig": "workspace:*",
    "typescript": "^5"
  }
}
